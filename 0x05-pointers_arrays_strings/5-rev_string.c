#include "main.h"
#include <stdio.h>

/**
 * rev_string - reverse string
 * @s: the string to be reversed
 */
void _strcat(char *c, char *s)
{
	int i, j;

	i = j = 0;
	while (c[i] != '\0')
		i++;

	while ((c[i++] = s[j++]) != '\0')
		;

}
void rev_string(char *s)
{
	int i, j;
	char temp[];
	while (s[j] != '\0')
		j++;
	
	while ()
}
